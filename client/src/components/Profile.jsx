import React from 'react'

const Profile = (props) => {
    return (
      <div>
        <h1>Hello User</h1>
        {/* {this.props.user
          &&
          this.props.user.map(name => {
            return (
              
              <div>{name.first_name}</div>
            )
          })
        } */}
      </div>
    )
  }

export default Profile;